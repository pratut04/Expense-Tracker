# Production environment — committed to git so Vercel uses this during build
# Vite automatically uses .env.production when building for production
VITE_API_URL=https://expense-tracker-d2zx.onrender.com/api
